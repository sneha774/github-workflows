name: Testing Trigger - Schedule

on:
  schedule:
    - cron: "0/5 * * * *"

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Every time
        run: echo "This step will always run"
